@page

@attribute [Route(RoutingContants.Billing.Default)


@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo(RoutingContants.Billing.Manage);
    }
}