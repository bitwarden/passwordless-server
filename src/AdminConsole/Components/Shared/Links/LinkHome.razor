@inherits BaseRazorComponent

<a class="@Class" href="">
    @if (IsDesktop)
    {
        <LogoFull />
    }
    else
    {
        <LogoMobile />
    }
</a>

@code
{
    [Parameter]
    public RenderFragment ChildContent { get; set; }

    [Parameter]
    public bool IsDesktop { get; set; }
}